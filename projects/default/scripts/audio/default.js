function audioNode(graph, helpers, audioInNode, audioOutNode, outputFrame) {

  const audioContext = graph.como.audioContext;
  // @example
  // var synth = new helpers.synths.LoopSynth();
  // synth.connect(audioOutNode);

  return {
    updateParams(updates) {

    },
    process(inputFrame) {
      
    },
    destroy() {

    },
  }
}